改进后的设计（v2）

1. 线程安全 - 使用并发容器

public class BackgroundTaskManager {
// 任务状态（线程安全）
private final ConcurrentHashMap<String, TaskInfo> tasks = new ConcurrentHashMap<>();
// 通知队列（阻塞队列，线程安全）
private final BlockingQueue<TaskNotification> notificationQueue = new LinkedBlockingQueue<>();

      // 守护线程跟踪（用于优雅关闭）
      private final Set<Thread> activeThreads = ConcurrentHashMap.newKeySet();

      public List<TaskNotification> drainNotifications() {
          List<TaskNotification> result = new ArrayList<>();
          notificationQueue.drainTo(result);  // 原子操作
          return result;
      }
}

  ---
2. 资源管理 - 限制线程池 + 自动清理

// PooledExecutor 改进版
class PooledExecutor implements TaskExecutor {
private final ExecutorService pool;
public PooledExecutor(int maxTasks) {
this.pool = new ThreadPoolExecutor(
0, maxTasks,                // 核心线程0，最大线程=maxTasks
60L, TimeUnit.SECONDS,
new SynchronousQueue<>(),
new ThreadFactory() {
public Thread newThread(Runnable r) {
Thread t = new Thread(r);
t.setDaemon(true);      // 守护线程
return t;
}
},
new ThreadPoolExecutor.CallerRunsPolicy()  // 背压：调用者线程执行
);
}

      public void shutdown() {
          pool.shutdown();
          try {
              if (!pool.awaitTermination(10, TimeUnit.SECONDS)) {
                  pool.shutdownNow();
              }
          } catch (InterruptedException e) {
              pool.shutdownNow();
              Thread.currentThread().interrupt();
          }
      }
}

// DaemonThreadExecutor 改进版
class DaemonThreadExecutor implements TaskExecutor {
private final Set<Thread> activeThreads = ConcurrentHashMap.newKeySet();
private final int maxTasks;

      public void execute(Runnable task, String taskId) {
          if (activeThreads.size() >= maxTasks) {
              throw new RejectedExecutionException("Too many tasks: " + activeThreads.size());
          }
          Thread t = new Thread(() -> {
              try {
                  task.run();
              } finally {
                  activeThreads.remove(Thread.currentThread());
              }
          }, "bg-" + taskId);
          t.setDaemon(true);
          activeThreads.add(t);
          t.start();
      }
      public void shutdown() {
          for (Thread t : activeThreads) {
              t.interrupt();
          }
      }
}

  ---
3. 输出收集 - StreamGobbler

private String captureOutput(Process process, int maxChars) throws IOException {
StringBuilder stdout = new StringBuilder();
StringBuilder stderr = new StringBuilder();

      // 启动两个线程分别读取 stdout 和 stderr
      Thread stdoutReader = new Thread(() -> {
          try (BufferedReader reader = new BufferedReader(
                  new InputStreamReader(process.getInputStream(), StandardCharsets.UTF_8))) {
              String line;
              while ((line = reader.readLine()) != null && stdout.length() < maxChars) {
                  stdout.append(line).append("\n");
              }
          } catch (IOException e) {
              // ignore
          }
      });
      Thread stderrReader = new Thread(() -> {
          try (BufferedReader reader = new BufferedReader(
                  new InputStreamReader(process.getErrorStream(), StandardCharsets.UTF_8))) {
              String line;
              while ((line = reader.readLine()) != null && stderr.length() < maxChars) {
                  stderr.append(line).append("\n");
              }
          } catch (IOException e) {
              // ignore
          }
      });

      stdoutReader.start();
      stderrReader.start();

      // 等待读取完成
      try {
          stdoutReader.join();
          stderrReader.join();
      } catch (InteruptedException e) {
          Thread.currentThread().interrupt();
      }

      String combined = stdout.toString() + stderr.toString();
      if (combined.length() > maxChars) {
          return combined.substring(0, maxChars) + "\n... (truncated)";
      }
      return combined;
}

  ---
4. 状态机 + 错误分类

enum TaskStatus {
PENDING,        // 刚创建，未启动
RUNNING,        // 正在执行
COMPLETED,      // 成功完成（exit code 0）
FAILED,         // 执行失败（exit code != 0）
TIMEOUT,        // 超时被终止
CANCELLED,      // 用户取消
START_FAILED    // 启动失败（command not found, permission denied）
}

class TaskInfo {
String taskId;
String command;
String workDir;
TaskStatus status;
Process process;
Instant startTime;
Instant endTime;
int exitCode;
String output;
String errorMessage;
// 状态转换验证
public void transitionTo(TaskStatus newStatus) {
if (!isValidTransition(this.status, newStatus)) {
throw new IllegalStateException(
"Invalid transition: " + this.status + " -> " + newStatus);
}
this.status = newStatus;
}

      private boolean isValidTransition(TaskStatus from, TaskStatus to) {
          switch (from) {
              case PENDING: return to == TaskStatus.RUNNING || to == TaskStatus.START_FAILED;
              case RUNNING: return to == TaskStatus.COMPLETED || to == TaskStatus.FAILED
                                || to == TaskStatus.TIMEOUT || to == TaskStatus.CANCELLED;
              default: return false;  // 终态不能转换
          }
      }
}

  ---
5. Windows 兼容性

private ProcessBuilder createProcessBuilder(String command, String workDir) {
ProcessBuilder pb;
if (System.getProperty("os.name").toLowerCase().contains("win")) {
// Windows: 使用 cmd.exe /c
pb = new ProcessBuilder("cmd.exe", "/c", command);
} else {
// Unix: 使用 bash -c
pb = new ProcessBuilder("bash", "-c", command);
}

      pb.directory(new File(workDir));
      pb.redirectErrorStream(false);  // 分别收集 stdout 和 stderr
      return pb;
}

  ---
6. 配置验证

// AppConfig.java
public static int getBackgroundTimeoutSeconds() {
int timeout = getInt("BACKGROUND_TIMEOUT_SECONDS", 300);
if (timeout <= 0 || timeout > 3600) {
throw new IllegalArgumentException(
"BACKGROUND_TIMEOUT_SECONDS must be in [1, 3600], got: " + timeout);
}
return timeout;
}

public static int getBackgroundMaxTasks() {
int max = getInt("BACKGROUND_MAX_TASKS", 10);
if (max < 1 || max > 50) {
throw new IllegalArgumentException(
"BACKGROUND_MAX_TASKS must be in [1, 50], got: " + max);
}
return max;
}

  ---
7. 任务清理机制

// 定期清理已完成任务（避免内存泄漏）
private finalScheduledExecutorService cleaner = Executors.newSingleThreadScheduledExecutor();

public BackgroundTaskManager() {
// ...
// 每 5 分钟清理一次超过 1 小时的已完成任务
cleaner.scheduleAtFixedRate(this::cleanupOldTasks, 5, TimeUnit.MINUTES);
}

private void cleanupOldTasks() {
Instant cutoff = Instant.now().minus(1, ChronoUnit.HOURS);
tasks.entrySet().removeIf(entry -> {
TaskInfo info = entry.getValue();
return info.status.isTerminal() && info.endTime.isBefore(cutoff);
});
}

  ---
8. 完整的 TaskNotification

public class TaskNotification {
private final String taskId;
private final TaskStatus status;
private final long durationMs;
private final String summary;      // 500 字符摘要
private final int exitCode;
private final Instant startTime;
private final Instant endTime;
private final String errorMessage;
private final String command;

      // 构造器+ geters
}

  ---
最终确认清单（v2）

| 问题 | 解决方案 | 状态 |
|------|------|
| 线程安全 | ConcurrentHashMap + BlockingQueue | ✅ |
| 资源泄漏 | ThreadPoolExecutor 限制 + StreamGobbler 自动关闭 | ✅ |
| DaemonThreadExecutor | 跟踪线程 + interrupt() 关闭 | ✅ |
| 输出收集 | StreamGobbler 双线程读取 | ✅ |
| 错误分类 | 6 种状态 + 状态机验证 | ✅ |
| 配置验证 | 范围检查 + 异常抛出 | ✅ |
| TaskNotification | 完整字段（exitCode, errorMessage, command） | ✅ |
| Windows 兼容 | cmd.exe /c 包装 | ✅ |
| 任务清理 | ScheduledExecutorService 定期清理 | ✅ |
| 背压机制 | CalerRunsPolicy（线程池）/ RejectedExecutionException（守护线程） | ✅ |

  ---
实现顺序（改进版）

1. TaskStatus 枚举 + TaskInfo 类（状态机）
2. TaskNotification 类（完整字段）
3. TaskExecutor 接口 + 两个实现（线程安全 + 资源管理）
4. BackgroundTaskManager（核心逻辑 + 清理机制）
5. 三个工具（BackgroundRunTool, BackgroundCheckTool, BackgroundCancelTool）
6. AppConfig 配置项（验证逻辑）
7. AgentLoop 集成（injectBackgroundNotifications）
8. ToolRegistry 注册
9. 测试（启动、取消、超时、并发）